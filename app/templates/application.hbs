{{#custom-form/ui-form onSubmit=(action "save") as |form|}}
  {{#form.label}}
    User Name:
  {{/form.label}}
  {{form.input placeholder="my input" value=name}}

  {{#custom-form/ui-button onClick=(action (mut name) "")}}
    Reset
  {{/custom-form/ui-button}}

  {{#form.submit}}
    save
  {{/form.submit}}
{{/custom-form/ui-form}}